package com.yyf.utils;

public enum EsRootOptions {
	_source, aggs, form, hightlight, post_filter, query, scripts_fields, size, sort, sorted_fields, timeout, version, docvalue_fields, explain, partial_fields, stats

}
