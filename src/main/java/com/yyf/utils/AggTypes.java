package com.yyf.utils;

public enum AggTypes {
	aggs, avg, avg_bucket, bucket_script, bucket_select, children, filter, filters, geo_bounds, cardinality, date_histogram, date_range, cumulative_sum, min, min_bucket, derivative, missing, nested, percentiles, geohash_grid, global, martix_stats, reverse_nested, extended_stats, max, geo_distance, ip_range, max_bucket, scripted_metric, histogram, sum_bucket, sum, terms, top_hits, range, moving_avg, sampler, serial_diff, stats, percentile_ranks, value_count, significant_terms
}
